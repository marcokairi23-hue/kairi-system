# CLAUDE.md — הוראות קבועות לפרויקט קאירי

קרא את `docs/HANDOFF.md` בתחילת כל session. הוא נקודת הכניסה.

## כללי עבודה (מלאים ב-docs/WORKPLAN.md)
- מקור אמת = קבצי docs/. לא זיכרון, לא שיחה.
- אישור לפני כל שינוי בקוד. הצג תוכנית → אשר → בצע.
- שלב-אחד-בכל-פעם. עצור לאישור בסוף כל שלב.
- git commit checkpoint לפני כל פעולה מסוכנת (מחיקת נתונים/סכמה).
- גע רק בקבצים של המשימה הנוכחית. אל תסרוק את כל ה-repo.
- כל שינוי סכמה = migration file, לא Supabase UI.
- עדכן docs/HANDOFF.md אחרי כל שלב שהושלם (סמן [x], עדכן מצב+commits).

## עקרונות
1. אחריות ושקיפות — כל פעולה עם חותמת זמן ומבצע.
2. גמיש אך ערני — לא חוסמים; מדגלים חריגות.
3. דאטה קודם — מדד ההצלחה = מהירות ההטמעה.

## סטק
React 18 + Vite + TS + Tailwind · Supabase (ref ipcnyqkcvbvzmasmzaur) · Cloudflare Pages.
עברית/RTL בלבד.
