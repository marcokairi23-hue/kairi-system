# SPEC.md — אפיון מערכת קאירי

> אפיון המערכת מא' עד ת'. מקור אמת יחיד למה שנבנה. משלים את `BUSINESS.md` (שמתאר את העסק).
> עודכן: 11.08.2026 · בעלים: מרקו קאירי
> **כלל:** סעיף נכנס ל-Phase 1 רק לאחר אישור מפורש. רעיונות עתידיים ב-`IDEAS.md`.

---

## 0. שלושת עקרונות היסוד

1. **אחריות ושקיפות** — כל פעולה עם חותמת זמן ומבצע (`changed_by`, `created_at`). ה-`changed_by` חייב להישמר בכל מקום — פער כאן מנטרל את מטרת המערכת.
2. **גמיש אך ערני** — לא חוסמים; מדגלים ומתעדים חריגות. חריג יחיד שחוסם: ביטול לאחר כניסה לייצור.
3. **דאטה קודם, תחכום אחר כך** — לוכדים נתונים עכשיו, בונים מנגנונים אחר כך. נתון שלא נלכד — אבוד לנצח.

---

## 1. מודל הסטטוסים

### 1.1 סטטוס הזמנה
```
הצעת מחיר → ממתין לגבייה/אישור → [ייצור] → מוכנה → נאסף ע"י מתקין → הושלמה
```
- ביטול אפשרי רק בשני הסטטוסים הראשונים. מכניסה לייצור — חסום.
- אין "בוטלה" כמצב שאליו מגיעים מכל שלב.

### 1.2 סטטוס פריט
```
(ריק לפני SUBMIT) → הצעת מחיר → חדש לביצוע → נגזר → במתפרה → מוכן
```
- לפני SUBMIT: מצב פריט ריק (לא "חדש").
- בבחירת "הצעת מחיר": כל הפריטים = "הצעת מחיר".
- פריט עובר ל"חדש לביצוע" רק אם סומן ב-checkbox בכניסה לייצור.
- **פערים לאימות מול הקוד:** (א) סטטוס פריט "הצעת מחיר" אולי לא קיים ב-enum. (ב) היום ה-enum מאפס פריטים ל"חדש" — צריך לתקן לסלקטיבי.

### 1.3 סנכרון דו-כיווני (קיים, לאימות)
- קידום הזמנה → דיאלוג "לעדכן N פריטים?"
- כל הפריטים באותו סטטוס → דיאלוג "לקדם הזמנה?"
- הזמנה חלקית = פס התקדמות (4/6), לא סטטוס נפרד.

---

## 2. לוגיקת הליבה (Phase 1 — חלק א')

### 2.1 דיאלוג SUBMIT
בחירה בין: **הצעת מחיר** (כל הפריטים "הצעת מחיר", ללא פעולה) / **ממתין לגבייה** (קליטת משרד + מקדמה).

### 2.2 חלון ביטול
- אפשרי ב"הצעת מחיר" / "ממתין לגבייה" בלבד.
- מכניסה לייצור — המערכת **חוסמת** ביטול (custom made).
- ביטול = END POINT; סיבה נרשמת בטקסט חופשי.

### 2.3 כניסה לייצור סלקטיבית
- רק לאחר אישור משרד.
- רק פריטים מסומנים ב-checkbox עוברים ל"חדש לביצוע"; השאר נשארים "הצעת מחיר".

### 2.4 שני מסלולי ייצור מקבילים (לפי `family`)
- **curtain** → גוזר (יוני): גזירה לפי נוסחה ידנית → מתפרה → מוכן.
- **shading** → משרד מזמין מספק (וואטסאפ) → מוכן.
- כל פריט: תיעוד **תאריך שהפך למוכן**.

### 2.5 מעבר הזמנה ל"מוכנה"
- **רגיל:** סימון פריט אחרון כמוכן → דיאלוג "לסמן הזמנה כמוכנה?".
- **override:** קידום למרות פריטים לא-מוכנים → דיאלוג עם כל הפריטים + checkbox לכל אחד + "בחר הכל".

### 2.6 איסוף / התקנה
- בחירה בין שני ענפים בהזמנה מוכנה.
- **איסוף:** כל אחד מסוגל להחתים ולסגור יתרה.
- **התקנה:** שיוך מתקין **ויזואלית בולט**. התראה במערכת בלבד (WA בהמשך). מתקין מקבל טופס מלא עם מחירים. סיום: **שתי חתימות** (לקוח + מתקין).

### 2.7 שני אירועי כסף (לכידה בלבד)
כל תשלום נושא:
- `collected_by` — מי גבה פיזית (סוכן X / משרד / מתקין).
- `handed_over` — בוליאני: הועבר לקופה?
תשלום מזומן/צ'ק אינו "סגור" עד `handed_over = true`. **לכידה בלבד — ללא מסך/מנגנון בשלב זה.**

### 2.8 תיקוני יושרת דאטה (Phase 1 — תנאי סף)
מבוסס על `DEFECTS_MAP.md` (19 ליקויים). אלה אינם פיצ'רים — הם תנאי סף להטמעה, כי בלעדיהם הדאטה שנלכד מזוהם או פרוץ. מקור אמת מלא: `DEFECTS_MAP.md`.

**קבוצה 1 — תנאי סף (חובה):**
- **#5 (🔴 חוסם):** `EditOrder` עושה DELETE+INSERT לכל הפריטים, מתעלם משגיאת FK (`order_status_history` ללא CASCADE) → פריטים כפולים בשקט + היסטוריה שבורה. תיקון: UPDATE לפריטים קיימים במקום DELETE+INSERT, או בדיקת שגיאה + עצירה.
- **#15 (משבש):** `order_status_history` פרוץ — RLS דורש רק `auth.uid() is not null`, כל משתמש מזייף רשומות ביקורת. פרצה בלב האחריות. תיקון: הגבלת RLS לפי בעלות על ההזמנה.
- **#1 (משבש):** כל הזמנה יוצרת לקוח חדש ללא חיפוש (טלפון עם 23 רשומות). תיקון: חיפוש לקוח לפי טלפון לפני יצירה.
- **#4 (משבש):** checkbox "הצעת מחיר" ב-NewOrder מנותק מהשמירה (§2.1). תיקון: לחבר או להסיר.
- **#3 (משבש):** שדה גבהים מסנן ערכים בשקט (כולל 0 לגיטימי). **לאחד עם מעבר היחידות §4.1.**

**קבוצה 2 — לתקן תוך כדי (אותם אזורי קוד):**
- **#7:** OrderDetail מקדם סטטוס בלי סנכרון דו-כיווני (מתחבר §2.5).
- **#9:** ActivityLog בלי FK מפורש (אותו קובץ כמו #15).
- **#12:** ShareButton מת → route `/print/` לא קיים (מתחבר §4.6 — מוחקים).
- **#2:** אביזרים לא נשמרים ל-DB (NewOrder/EditOrder שממילא נערוך).

**נדחה ל-`IDEAS.md`:** RLS של sales (#13,#14,#16) · ניקוי סכמה (#8,#10,#11,#18) · נתוני דמו מזוהמים (#6,#17,#19 — ייעלמו במחיקת הסרק, לא באגי קוד).

### 2.9 תפקיד מתקין — משתמש מלא (Phase 1: תשתית בלבד)
המתקין הופך מ"שם ברשימה" (settings) ל**משתמש אמיתי** ב-`profiles`, כדי שפעולותיו (חתימה, סיום התקנה) יהיו מתועדות ואחראיות — קריטי כי המתקין גובה כסף ישירות מהלקוח (§2.6).

**חצי א' — תשתית (Phase 1):**
- הוספת `installer` ל-enum `user_role`.
- יצירת משתמש מתקין דרך ניהול המשתמשים הקיים (`UsersList.tsx` + Edge Function `create-user`).
- שיוך הזמנה למתקין → מצביע על `profiles` שלו (במקום/בנוסף לשם ב-settings §2.6).
- ההגנה הקיימת על היומן (אי-מחיקת משתמש שביצע פעולה) חלה אוטומטית גם על מתקינים.

**חצי ב' — RLS מדויק (→ `IDEAS.md`, שלב ב' קרוב):**
- הגדרת בדיוק מה מתקין רואה כשנכנס: רק הזמנות משויכות, מחירים (כן — §2.6), הזמנות/לקוחות אחרים (לא), רמות אמון.
- **למה נדחה:** דורש עיצוב RLS זהיר, ועדיף לעשותו יחד עם תיקון באגי RLS הקיימים (#13,#14,#15) בשלב מרוכז.
- **פער לפתירה:** האם שיוך המתקין (§2.6, dropdown מ-settings) מוחלף בבחירת משתמש `profiles`, או ששתי הרשימות מתקיימות במקביל. לאמת מול הקוד.

> **ממצא מ-`ARCHITECTURE.md`:** רוב חלק ב' **כבר בנוי** בקוד. המשימה כאן היא לאמת ולתקן, לא לבנות מאפס.

### 3.1 יומן פעילות (עדיפות עליונה) — קיים, לאמת
- **כבר בנוי:** `ActivityLog.tsx`, `OrderActivityTab.tsx`, לשונית "יומן פעילות" ב-`OrderDetail.tsx`, פונקציית `fetchActivity()` שמאחדת `order_status_history` + `payments`.
- **לתקן:** פרצת RLS (#15) + FK מפורש (#9) — ראה §2.8.
- פורמט: `תאריך שעה · שם · פעולה`.

### 3.2 ניהול משתמשים — קיים, לאמת
- **כבר בנוי:** `UsersList.tsx` + Edge Functions `create-user` / `delete-user` (עם אבטחת admin בלבד, JWT).
- **מנגנון הגנה קיים (חשוב):** מחיקת משתמש שביצע פעולה **נכשלת בכוונה** (FK ללא CASCADE) כדי לא לאבד את יומן "מי עשה מה" — מפנה ל-`is_active=false`. זה מימוש ישיר של עקרון האחריות.
- **לאמת:** שהוספת רן/גיא/אליזבט עובדת חלק מה-UI.

---

## 4. תיקוני UI וקומפוננטות (Phase 1 — חלק ג')

### 4.1 יחידות מידה — החלטה ארכיטקטונית (קריטי)
**אחסון במטרים, המרה בתצוגה בלבד. שמות עמודות אמינים.**

| שדה ישן (ס"מ) | שדה חדש (מטר) | תצוגה בטופס | אחסון |
|---|---|---|---|
| `width_cm` | `width_m` | 4.01 מ' | 4.01 |
| `heights_cm` (מערך) | `heights_m` (מערך) | 3.06 מ' | 3.06 |
| `shtaif_cm` | `shtaif_m` | 10 ס"מ | 0.10 |
| `hem_cm` | `hem_m` | 10 ס"מ | 0.10 |

- **רוחב/גובה:** קלט ותצוגה במטרים, אחסון אחד-לאחד. תואם לעבודה בשטח.
- **שטייף/מכפלת:** קלט ותצוגה בס"מ, אחסון במטרים (÷100). **התצוגה תמיד כפי שנרשם.**
- **שכבת המרה מרוכזת:** פונקציות `cmToM` / `mToCm` במקום אחד (helper), כל הטפסים משתמשים בהן.
- **נתונים קיימים:** ~27 פריטי סרק — **נמחקים** כחלק מהמעבר (לא המרה). אפס סיכון לשלמות נתונים. יבוצע ע"י CC עם `git commit` checkpoint לפני.
- **החלטה:** נדחתה גישת עמודות מחושבות (`GENERATED`) — מוסיפה כפילות ובלבול בלי תועלת במקרה זה (במיוחד על `heights` שהוא מערך).

### 4.2 תיקונים חוצי-טפסים (וילון + הצללה)
- כפתור "הוסף וילון" / "הוסף פריט" → יעבור ל**תחתית** הכרטיס.
- שדות רוחב/גובה → מטרים (ראה 4.1).

### 4.3 טופס וילון
- "חצוי" → בולט יותר (checkbox מודגש או המרה ל-select/dropdown).
- שטייף/מכפלת → תצוגת ס"מ, אחסון מטר (ראה 4.1).
- סוג תפירה → הערכים ניתנים לעריכה מעמוד ההגדרות (ראה 4.5).

### 4.4 טופס הצללה
- סוג פריט (זברה/ונציאני/רומי/גלילה) → ניתן להוסיף/לערוך מעמוד ההגדרות (ראה 4.5).
- רוחב/גובה → מטרים; כפתורים → אותם דגשים.

### 4.5 עמוד הגדרות — שינוי כולל
הופך למרכז ניהול רשימות ערכים (lookup lists) הניתנות לעריכה:
- סוגי תפירה (שטוח הפוך, קפלים, טאבים, שרוול, אחר...)
- סוגי פריט הצללה (זברה, ונציאני, רומי, גלילה...)
- רשימת מתקינים (קיים בסיסית — ראה תמונה)
- אמצעי תשלום
- (קיים: מונה הזמנות, טקסט משפטי)

### 4.6 מנגנון PDF — מקור קפוא + הדפסה על-פי-דרישה
המצב היום: שני מנגנוני PDF במסך ההזמנה, אחד עובד ואחד שבור.

- **PDF מקור (קפוא לצמיתות) — נשאר.** קובץ `-original.pdf` נשמר ב-Supabase Storage. זו האסמכתא/העותק המקורי של ההזמנה. **נוצר ברגע המעבר "ממתין לגבייה → חדש לייצור".** לא משתנה לעולם. הקישור הציבורי מצביע עליו.
- **PDF חי (מסונכרן) + כפתור "סנכרן PDF" — נמחקים.** שניהם שבורים (הקישור החי לא עובד, הסנכרון לא עובד). חוב טכני להסרה.
- **מחליף אותם:** כפתור "הדפס ללקוח" הקיים (העליון) — מייצר מסמך מעודכן **על-פי-דרישה ברגע הלחיצה** (on-demand), עם הפרטים הנוכחיים. לא נשמר כקובץ. כבר עובד היום (מפיק מסמך טרי ומעודכן).
- **סיכום המודל:** הקפוא נשמר כקובץ (אסמכתא); החי נוצר על-פי-דרישה ולא נשמר. אין "סנכרון".

### 4.7 ללא שינוי
- **תשלום וסיכום** — נשאר כפי שהוא.
- **פרטי לקוח** — נשאר כפי שהוא.

---

## 5. סכמת DB — שינויים נדרשים (מאומת מול `ARCHITECTURE.md`)

**שינויי Phase 1:**
- `user_role` enum: הוספת `installer` (§2.9).
- `order_items`: החלפת `width_cm`→`width_m`, `heights_cm`→`heights_m`, `shtaif_cm`→`shtaif_m`, `hem_cm`→`hem_m`.
- `payments`: הוספת `collected_by` + `handed_over`. **הערה:** כבר קיים `received_by` (FK יחיד ל-`profiles`) — לבדוק אם `collected_by` חופף אליו או משלים.
- `order_status_history`: תיקון RLS (#15) — הגבלה לפי בעלות במקום `auth.uid() is not null`.
- `customers`: לוגיקת חיפוש-לפני-יצירה (#1) — קוד, לא סכמה.

**עובדות DB מאומתות (מ-`ARCHITECTURE.md` §3):**
- **מיגרציות בקוד:** `0001`–`0006` (לא `0003` בלבד כפי שחשבנו). כולל `order-pdfs` bucket ותיקוני storage RLS.
- **ה-enum `item_status` בפועל:** `new, ordered_from_supplier, arrived, cut, sewing, ready, installed, cancelled` (8 ערכים). הצד-לקוח מכיר רק 6 (#8). **אין ערך "הצעת מחיר"** — צריך להוסיף אם רוצים אותו (§1.2).
- **ה-enum `order_status`:** כולל `draft` (ברירת מחדל DB, לא בזרימה) ו-`ready_for_install`.
- **FK כפול:** רק `orders`→`profiles` דורש FK מפורש (`profiles!orders_agent_id_fkey`). `order_status_history.changed_by` ו-`payments.received_by` הם FK יחיד — לא צריך.
- **RLS על `settings`:** admin בלבד, לא office — לזכור אם office צריך לערוך רשימות ערכים (§4.5)!
- **חוב ספרינט הייצור (ב-DB, לא בקוד, לא מזיק):** טבלת `suppliers` מלאה, עמודות `production_route`/`supplier_id`/`date_*` ב-`order_items`, ו-13 טבלאות לא-בשימוש. **מעולם לא נכתב כמיגרציה** — קיים רק ב-DB. יטופל בהמשך.

> כל שינוי סכמה = migration file, לא Supabase UI. commit לפני.
> ⚠️ RLS על `settings` מוגבל ל-admin — קריטי לבדוק לפני §4.5 (עריכת רשימות ערכים ע"י office).

---

## 6. גבולות Phase 1

**בפנים:** לוגיקת ליבה (§2) · יומן פעילות + ניהול משתמשים (§3) · תיקוני UI + יחידות + הגדרות (§4).
**בחוץ (→ `IDEAS.md`):** מנגנון דגלים · מעקב ספק · מודול פגומים/עודפים · מסך כספים על סוכנים/מתקינים · ריבוי מתקינים · קטלוג עם תיוג · מנוע צריכת בד · פיצול דיאלוג לאמצעי תשלום · וריאציות בהצעת מחיר · חתימת canvas · WhatsApp/ManyChat.

---

## 7. יומן שינויים

- 11.08.2026 — נוצר. אופיינו: מודל סטטוסים, לוגיקת ליבה, לכידת דאטה, תיקוני UI (חלק ג'), והחלטת יחידות המידה (אחסון במטרים + שמות אמינים + מחיקת סרק).
- 11.08.2026 — נוסף §4.6: מנגנון PDF. חקירה גילתה PDF מקור קפוא (עובד, נשמר) + PDF חי/סנכרון (שבור). החלטה: להסיר את השבור, להשאיר הדפסה על-פי-דרישה דרך "הדפס ללקוח". מקור קפוא נוצר במעבר ממתין לגבייה→חדש לייצור.
- 11.08.2026 — הצלבה מול `ARCHITECTURE.md` + `DEFECTS_MAP.md`. עדכונים: §2.8 (תיקוני יושרת דאטה — קבוצה 1+2, 9 באגים), §3 (יומן ומשתמשים כבר בנויים — לאמת ולא לבנות), §5 (עובדות DB מאומתות: מיגרציות 0001-0006, enum בפועל, RLS על settings=admin, FK כפול). באגים נדחים → IDEAS.
- 11.08.2026 — נוסף §2.9: תפקיד מתקין כמשתמש מלא. חצי א (תשתית: enum installer + יצירת משתמש + שיוך) ב-Phase 1; חצי ב (RLS מדויק) → IDEAS. עודכן §5.
